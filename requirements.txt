# Auto Reconnaissance Tool - Requirements
# Core dependencies for the Python reconnaissance tool

# External tools required:
# - subfinder (install via: go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest)
# - amass (install via: brew install amass or apt-get install amass)
# - httpx (install via: go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest)
# - waybackurls (install via: go install -v github.com/tomnomnom/waybackurls@latest)
# - gau (install via: go install -v github.com/lc/gau/v2/cmd/gau@latest)
# - jq (install via: brew install jq or apt-get install jq)
# - rengine (install via: https://github.com/yogeshojha/rengine)
# - osmedeus (install via: https://github.com/j3ssie/osmedeus)
# - porch-pirate (install via: pipx install porch-pirate)

# Python dependencies:
requests>=2.28.0
colorama>=0.4.4
tqdm>=4.64.0
python-dotenv>=0.19.0  # For environment variables
docker>=5.0.0  # For ReNgine and Osmedeus integration
